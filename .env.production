# En producción el frontend lo sirve el propio backend, así que la API y el
# WebSocket viven en el MISMO origen. Dejamos estas variables vacías para que
# el cliente use automáticamente window.location (mismo dominio).
VITE_API_URL=
VITE_WS_URL=
