// Constantes de UI para PANEL RADIO ONLINE.
// No contiene datos de demostración — todo se carga desde la API.

// Cuenta fallback (solo se usa si el usuario no está autenticado aún).
export const cuenta = {
  usuario: "",
  nombre: "Usuario",
  plan: "—",
  rol: "cliente",
};

// Opciones disponibles para configuración de audio.
export const formatosSoportados = ["MP3", "AAC", "AAC+", "Ogg Vorbis", "Opus"];
export const bitratesSoportados = [64, 96, 128, 192, 256, 320];
